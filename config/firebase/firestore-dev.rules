rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 开发阶段：允许所有认证用户读写
    // 生产环境请使用更严格的规则
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
